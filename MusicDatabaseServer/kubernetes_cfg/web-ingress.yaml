apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: web-ingress
  #TODO: create namespace
  #namespace: musicdb
spec:
  rules:
  - http:
      paths:
      - backend:
          serviceName: web
          #TODO: read from env vars instead of hardcode it here
          servicePort: 2020